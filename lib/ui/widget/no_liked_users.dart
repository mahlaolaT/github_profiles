import 'package:flutter/material.dart';

class NoLikedUsers extends StatelessWidget {
  const NoLikedUsers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('No Liked users.'));
  }
}
